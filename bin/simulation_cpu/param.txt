user_name = marcell								// User Name that will be used in the file name.
number_pacing = 20						// Number of paces.
number_pacing_write = 20       // starting pace number for writing.
cycle_length = 500.0				// Time period of one cycle (msec).
cell_model = Grandi	  // Name of the cell model. Refer to the guide to fill it
stimulus_duration = 5.0								// Duration of stimulus current applied (msec).
stimulus_amplitude_scale = 1.0				// Scaling factor of stimulus current amplitude.
solver_type = CVode										// Type of ODE solver used. Only CVode or Euler are available.
time_step_min = 0.1								// The minimum time step of the computation (msec). Only for CVode solver.
time_step_max = 0.1								// The maximum time step of the computation (msec). Only for CVode solver.
writing_step = 0.1								// The interval time to write the result into the file (msec).
gks_scale = 1.                    // The scaling factor of gKs_max conductance.
gkr_scale = 1.                    // The scaling factor of gKr_max conductance.
gk1_scale = 1.                    // The scaling factor of gK1_max conductance.
gto_scale = 1.                    // The scaling factor of gto_max conductance.
gna_scale = 1.                    // The scaling factor of gNa_max conductance.
gnal_scale = 1.                   // The scaling factor of gNaL_max conductance.
pnab_scale = 1.                   // The scaling factor of gNaB conductance.
pcab_scale = 1.                   // The scaling factor of gCaB conductance.
